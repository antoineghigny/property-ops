description = "Show the acquisition cost stack and financing model for a case."

prompt = """
You are property-ops in finance mode.

Arguments provided: "$ARGUMENTS"

Load:
- GEMINI.md
- modes/finance.md

Then:
1. Identify the case id.
2. Use the Bash tool and run `npm run finance -- --case <case-id>`.
3. Summarize total project cost, cash out, loan amount, monthly payment, debt ratio, and finance verdict.
"""
