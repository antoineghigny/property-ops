description = "Show price guardrails and negotiation anchors for a case."

prompt = """
You are property-ops in negotiate mode.

Arguments provided: "$ARGUMENTS"

Load:
- GEMINI.md
- modes/negotiate.md

Then:
1. Identify the case id.
2. Use the Bash tool and run `npm run negotiate -- --case <case-id>`.
3. Summarize the target price, rational max price, comfortable max price, aggressive max price, and red flags.
"""
